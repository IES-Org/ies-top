# Development Log

## Changes

- [x] Update `github.py` to handle PR creation and updates in line with revised branch flows `docs/diagrams/dev-workflow-complex.mmd`
- [ ] Update `create-pr.yml` to automate PR creation and initial setup
- [ ] Update `pr-checks.yml` to add/remove labels for each PR type
- [x] Add branch protection rules to `github.py` in IES Tools

## ToDo
- [ ] Update `trigger-pub-update.yml` to use `v[major.minor]` branches and copy to `ies-pub` repo for publication
- [ ] Update `pr-checks.yml` to:
    - [ ] Add `ready-for-review` label when PR is ready for review
    - [ ] Add `critical` label to `hotfix` PRs targeting `v[major.minor]` branches
    - [ ] Remove `ready-for-review` label when PR is closed
    - [ ] Close associated issue (only) when a `rc` PR is closed, i.e. a new release is approved.
    - [ ] merge `v[major.minor]` branches into `main` when a new release is approved. Tag the release and update the `main` branch version number, e.g. tag = `v[major.minor.patch+1]`, version = `v[major.minor.patch+1]`
- [ ] A guide to "Publishing the IES Ontology Family":
    - [ ] A new repository for published versions of the IES Ontology Family ...
    - [ ] ... or look into GitHub publishing features
- [ ] Implement `validate-ontology.yml` and add functionality to ies-tools - a new `validate.py`
- [ ] Update `pr-checks.yml` to carry out other automated checks in the processes, e.g.
    - [ ] Check for `critical` label on PRs targeting `main`
    - [ ] Remove `ready-for-review` label when PR is closed
    - [ ] Close associated issue when PR is closed
    - [ ] ]
- [ ] Modify ies-build build-ontology command to process all `.ttl` files in the ontology src directory, not just `ontology.ttl`
- [ ] Add comment to the associated issue when a PR is created, either in the `.github/workflows/pr-checks.yml` or in the gh-tools `github.py` script
- [ ] Add a `critical` label to the repository labels and associated issue workflows (`create-issue.yml`, `setup-repo.yml`, ...)
- [x] ~~Add `.github/workflows/create-pr.yml`~~
- [ ] Modify `github.py` and add `uopdate-pr.yml` to change a PR from draft to 'ready for review'
- [ ] Make sure `create-pr.yml` and `github.py` work when PR is targeting `qa/ -> rc/`
- [ ] Update docs to cover the version and other metadata changes to make upon release candidates, production release, and hotfixes
- [ ] Implement branch rules and other policy stuff, e.g.
    - [ ] Branch protection rules
    - [ ] Required status checks
    - [ ] Required review processes
    - [ ] Automated validation requirements
- [ ] Provide guidance on locally resolving ontology prefixes, e.g. IES Common (`ies:`) and domain-specific (e.g. `ies-building:`) using the submodule `common` and the `@prefix` directive in the ontology files:
    - [ ] need guidance on how to do it in VSCode and PyCharm and perhaps on the commandline
- [ ] Update docs on "external" collaborators and how to manage their contributions:
  - [ ] Read only viewers and tourists
  - [ ] Read/write contributors who are not part of a team
  - [ ] Users with permission to fork/clone the repo
- [ ] Create commit validation tools, e.g. add git hook for commitizen formatted commits
- [ ] additional checks to `pr-checks.yml`
- [ ] Create onboarding guides for each team role, i.e. developers, QA reviewers, TGG members, Managers
- [ ] Create team membership request process**
- [ ] Define team membership review criteria**
- [ ] Create access level decision matrix for teams**
- [ ] If required, add automated access review tools**
- [ ] Create hotfix PR template enforcing these requirements**
- [ ] Add hotfix validation checks to `pr-checks.yml`
    - [ ] Patch version number check
    - [ ] Size of change validation
    - [ ] Required documentation check
    - [ ] Test coverage verification
    - [ ] Semantic impact assessment
    - [ ] PR template section completion validation
    - [ ] Branch naming convention verification
    - [ ] File type modification restrictions
    - [ ] Required documentation file updates
- [ ] Create changelog entry template for hotfixes**
- [ ] Maintain a `KNOWN_ISSUES.md` file, delineated by VERSION
- [ ] Create reviewer checklist template
- [ ] Define metrics for tracking issues, PRs, bugfixes, hotfixes, and releases
- [ ] Create a template for documenting the narrative that goes with an ontology
- [ ] Add initial CODEOWNERS file
- [ ] Implement linear commit history (rebase required on merges)
- [ ] Write SECURITY.md file
- [ ] Update `gh-tools` to manage QA, RC, and Release PRs
- [ ] Create a new gh-tools workflow to validate ontology .ttl files
- [ ] New gh-tools command
   - [ ] to apply versions to ontology files and update relevant files like `VERSION`, `CHANGELOG.md`, etc.
   - [ ] Create a tool in `ies-tools` to automate generation of `CHANGELOG.md` entries
- [ ] Redraw the ontology layers diagram to show the layered, modular architecture, including third party IES extensions, "application" ontologies composed of common and domain concepts
- [ ] Add descriptions for: third party IES extensions, third party ontologies (e.g. GoeSparql, QUDT, ...), application ontologies (compositions with specific extensions for a use case, drawing on common and domain ontologies, as well as third party modules)**
- [ ] Describe the difference and use of root directory level `catalog.xml` and `imports/catalog-v001.xml`**
- [ ] Add automated branch name validation
- [ ] TODO: Create PR templates for each stage
- [ ] Add branch protection configuration guide
- [ ] Create backporting procedure document
- [ ] Add automated version number validation
- [ ] Create release notes template
- [ ] Update `github.py` to close issues when `rc` PRs are closed]
